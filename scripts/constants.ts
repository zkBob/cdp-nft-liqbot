import { BigNumber } from "ethers";

export const Q96 = BigNumber.from(2).pow(96);
export const Q48 = BigNumber.from(2).pow(48);
export const DENOMINATOR = BigNumber.from(10).pow(9);
export const DEBT_DENOMINATOR = BigNumber.from(10).pow(18);
